#!/bin/bash

TEMPDIR=`mktemp -d`
BABEL=node_modules/.bin/babel
BROWSERIFY=node_modules/.bin/browserify
OUT_FILE=kana-convert.min.js

rm -f $OUT_FILE
$BABEL src/*.js --presets minify,@babel/preset-env --out-dir $TEMPDIR
$BROWSERIFY --entry $TEMPDIR/browser.js --outfile $OUT_FILE

rm -rf $TEMPDIR
